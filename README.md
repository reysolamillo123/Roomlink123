
  # Modernize design elements

  This is a code bundle for Modernize design elements. The original project is available at https://www.figma.com/design/G26oyx5Ga7Ghycrbhwas3Y/Modernize-design-elements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  